To run this ray tracer you can use the following steps.
 ~ Create a build folder and locate that using the CMake.
 ~ Press configure followed by generate in cmake.
 ~ Go inside the build folder and run the Ray_Tracer.sln file.
 ~ requirements: You need to install eigen and OpenCV library and place it in the external folder.
